go.property("shoot_duration", 0.5)

function init(self)
	self.counter = self.shoot_duration
end

function update(self, dt)
	self.counter = self.counter - dt
	if self.counter <= 0 then
		self.counter = self.shoot_duration
		msg.post("cannon#fire_script", hash("fire"))
	end
end